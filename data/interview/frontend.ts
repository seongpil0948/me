import type { LocalizedInterviewQuestion } from "@/types/portfolio";

export const frontendQuestions: LocalizedInterviewQuestion[] = [
  {
    id: 20,
    category1: "Frontend",
    category2: "React",
    question: { ko: "React/Next.js 경험은 어떤가요?" },
    answer: {
      ko: "**React/Next.js 아키텍처 결정에서 가장 고민했던 부분은 렌더링 전략이었습니다.**\n\n**Pages Router → App Router 마이그레이션:**\n\n당시 Next.js 13 App Router를 도입할 때 가장 큰 딜레마는 '안정성 vs 미래 호환성'이었습니다.\n\n```typescript\n// Pages Router의 익숙함\nexport async function getServerSideProps() {\n  // 예측 가능한 동작\n}\n\n// vs App Router의 새로운 패러다임\nexport default async function Page() {\n  // Server Component - 서버에서만 실행\n  const data = await fetch('api/data');\n}\n```\n\n**Server Components vs Client Components 경계 설정:**\n\n가장 까다로운 것은 '어디서 boundary를 나눌 것인가'였습니다.\n서버에서 데이터를 가져오되, 상호작용은 클라이언트에서 처리해야 하는 딜레마:\n\n```typescript\n// 서버 컴포넌트에서 데이터 페칭\nasync function ServerDataProvider() {\n  const portfolioData = await getPortfolioData();\n  \n  return (\n    <ClientInteractiveComponent\n      initialData={portfolioData}\n      // serializable props만 전달 가능\n    />\n  );\n}\n```\n\n**Hydration Mismatch 해결:**\n\nSSR/SSG 환경에서 클라이언트 hydration 시 mismatch 문제:\n- 다크모드 전환: 서버는 'light', 클라이언트는 'dark' → FOUC 발생\n- 해결: Suspense boundary + 점진적 hydration\n\n**Performance 측정 결과:**\n- First Contentful Paint: 1.2s → 0.4s (App Router)\n- Bundle size: React 18 concurrent features로 10% 감소\n- Time to Interactive: Server Components로 JavaScript 번들 크기 30% 축소\n\n**현재 운영 중인 포트폴리오에서 배운 교훈:**\n1. **i18n routing**: middleware.ts가 아닌 proxy.ts 패턴 채택\n2. **Font loading**: FOUT 방지를 위한 CSS variable 기반 폰트 시스템\n3. **Component composition**: HeroUI 기반 design system 구축",
      en: "A concise React answer covering real production context, key decision trade-offs, measurable outcomes, and takeaways.",
    },
  },
  {
    id: 21,
    category1: "Frontend",
    category2: "Vue",
    question: { ko: "Vue.js 경험은 어떤가요?" },
    answer: {
      ko: "**Vue.js를 선택했던 이유는 '개발 생산성과 러닝 커브'였습니다.**\n\n**LG IXI Studio의 복잡한 상태 관리 도전:**\n\nGenerative AI 플랫폼에서 가장 어려웠던 것은 '실시간 AI 생성 상태'와 '사용자 인터랙션'의 동기화였습니다.\n\n```javascript\n// Vuex에서 WebSocket 상태와 UI 상태 분리\nconst store = new Vuex.Store({\n  modules: {\n    aiGeneration: {\n      state: { progress: 0, status: 'idle' },\n      mutations: {\n        UPDATE_PROGRESS(state, progress) {\n          // AI 진행률과 UI 동기화 로직\n        }\n      }\n    },\n    userInterface: {\n      // 사용자 상호작용 상태 분리\n    }\n  }\n});\n```\n\n**Virtual Try-on의 실시간 렌더링 최적화:**\n\nkonva.js 기반 캔버스 렌더링에서 성능 병목 해결:\n\n```javascript\n// Vue의 반응성 시스템이 canvas 조작에는 오버헤드\n// 해결: ref를 통한 직접 DOM 조작\nconst canvasRef = ref(null);\n\nconst optimizedRender = () => {\n  // Vue 반응성 시스템 우회\n  const canvas = canvasRef.value;\n  requestAnimationFrame(() => {\n    // 60fps 유지를 위한 직접 조작\n  });\n};\n```\n\n**Inoutbox POS의 오프라인 동기화:**\n\n가장 복잡했던 것은 '네트워크 단절 시 데이터 일관성'이었습니다:\n\n1. **Offline-first 아키텍처**: IndexedDB + Service Worker\n2. **Conflict resolution**: Last-write-wins vs Operational Transform\n3. **Vue의 computed property**: 로컬/원격 데이터 병합 로직\n\n```javascript\ncomputed: {\n  syncedTransactions() {\n    // 로컬 pending + 서버 confirmed 트랜잭션 병합\n    return [...this.localPending, ...this.serverConfirmed]\n      .sort((a, b) => a.timestamp - b.timestamp);\n  }\n}\n```\n\n**Vue 2 → Vue 3 Composition API 마이그레이션:**\n\nOptions API에서 Composition API로 전환 시:\n- **장점**: 로직 재사용성, TypeScript 호환성 향상\n- **도전**: 팀 내 러닝 커브, 기존 코드베이스 점진적 마이그레이션\n\n**Element UI vs 자체 컴포넌트 시스템:**\n\n초기에는 Element UI 사용 → 디자인 요구사항 증가로 자체 컴포넌트 개발\n결과: 번들 크기 40% 감소, 커스터마이징 자유도 확보",
      en: "A concise Vue answer covering real production context, key decision trade-offs, measurable outcomes, and takeaways.",
    },
  },
  {
    id: 22,
    category1: "Frontend",
    category2: "Build Tools",
    question: { ko: "Vite/빌드 도구 경험은 어떤가요?" },
    answer: {
      ko: "**빌드 도구 선택에서 가장 중요한 것은 '개발 경험과 빌드 성능의 균형'입니다.**\n\n**Vite의 CodeMirror Prerender 문제 해결:**\n\n가장 복잡했던 문제는 SSG(Static Site Generation) 환경에서 CodeMirror가 `document` 객체에 접근하는 이슈였습니다.\n\n```javascript\n// 문제: Node.js 환경에서 document 미정의\n// CodeMirror 내부에서 document.createElement() 호출\n\n// 해결책 1: Dynamic import (채택)\nconst CodeMirror = async () => {\n  if (typeof window !== 'undefined') {\n    const module = await import('codemirror');\n    return module.default;\n  }\n  return null;\n};\n\n// 해결책 2: Vite SSR externals (복잡함)\nexport default {\n  ssr: {\n    noExternal: ['codemirror']\n  }\n};\n```\n\n**Webpack → Vite 마이그레이션의 트레이드오프:**\n\n**성능 개선:**\n- Cold start: 45초 → 3초 (93% 개선)\n- HMR: 5초 → 200ms (96% 개선)\n- Bundle size: Tree-shaking 최적화로 15% 감소\n\n**마이그레이션 도전:**\n1. **CommonJS 의존성**: ESM으로 전환 필요\n2. **환경변수**: `process.env` → `import.meta.env`\n3. **Asset handling**: `require()` → `new URL()` pattern\n\n**Turbopack 도입 결정 과정:**\n\nNext.js 포트폴리오에서 Turbopack 선택 이유:\n```bash\n# Webpack (기존)\nnpm run dev → 8초 초기 빌드\n\n# Turbopack (현재)\npnpm dev → 1.2초 초기 빌드\n```\n\n하지만 trade-off도 있었습니다:\n- **장점**: Rust 기반 성능, Next.js 네이티브 지원\n- **단점**: 플러그인 생태계 제한, 디버깅 도구 부족\n\n**Bundle 분석과 최적화 전략:**\n\n```javascript\n// webpack-bundle-analyzer 결과 기반 최적화\nimport { lazy } from 'react';\n\n// Code splitting 전략\nconst HeavyComponent = lazy(() => \n  import('./HeavyComponent').then(module => ({\n    default: module.HeavyComponent\n  }))\n);\n\n// 결과: Initial bundle 250KB → 120KB\n```\n\n**현재 운영 중인 빌드 파이프라인:**\n\n1. **Development**: Turbopack (fast refresh)\n2. **Build**: Webpack (stable production build)\n3. **Linting**: ESLint + Prettier (pre-commit hook)\n4. **Type checking**: TypeScript noEmit 병렬 실행\n\n핵심은 '빌드 도구가 개발자의 플로우를 방해하지 않으면서도 최적화된 결과물을 생성'하는 것입니다.",
      en: "A concise Build Tools answer covering real production context, key decision trade-offs, measurable outcomes, and takeaways.",
    },
  },
  {
    id: 23,
    category1: "Frontend",
    category2: "Mobile",
    question: { ko: "Flutter/모바일 개발 경험은 어떤가요?" },
    answer: {
      ko: "**Flutter를 선택한 이유는 '하나의 코드베이스로 양 플랫폼 커버'였지만, 실제로는 플랫폼별 최적화가 핵심이었습니다.**\n\n**Campi의 Custom Image Editing 라이브러리 개발:**\n\n가장 도전적이었던 것은 '메모리 제약이 있는 모바일에서 대용량 이미지 처리'였습니다.\n\n```dart\n// 문제: 4000×3000 이미지 로딩 시 OutOfMemory\n// 해결: Tile-based processing\n\nclass TileImageProcessor {\n  Future<ui.Image> processLargeImage(File imageFile) async {\n    // 이미지를 512×512 타일로 분할\n    final tiles = await _splitIntoTiles(imageFile, 512);\n    \n    // 각 타일을 별도 isolate에서 처리\n    final processedTiles = await Future.wait(\n      tiles.map((tile) => compute(_processTile, tile))\n    );\n    \n    // 타일을 다시 합성\n    return _mergeTiles(processedTiles);\n  }\n}\n```\n\n**Gesture 인식의 수학적 구현:**\n\nPinch-to-zoom에서 aspect ratio 유지가 핵심이었습니다:\n\n```dart\nclass GestureHandler extends StatefulWidget {\n  void _onScaleUpdate(ScaleUpdateDetails details) {\n    // Transform matrix 계산\n    final matrix = Matrix4.identity()\n      ..translate(details.focalPoint.dx, details.focalPoint.dy)\n      ..scale(details.scale)\n      ..translate(-details.focalPoint.dx, -details.focalPoint.dy);\n    \n    // Boundary 검사로 이미지 경계 벗어남 방지\n    final transformedRect = _getTransformedRect(matrix);\n    if (_isWithinBounds(transformedRect)) {\n      setState(() => _transform = matrix);\n    }\n  }\n}\n```\n\n**Cross-Platform Architecture 전략:**\n\n**Platform Channel vs Plugin 선택:**\n\n```dart\n// Native Android 카메라 최적화가 필요한 부분\nclass CameraPlugin {\n  static const platform = MethodChannel('campi/camera');\n  \n  // Android: Camera2 API 직접 사용\n  // iOS: AVCaptureSession 커스터마이징\n  Future<String> captureOptimized() async {\n    return await platform.invokeMethod('captureWithSettings', {\n      'quality': 'high',\n      'format': 'jpeg',\n      'compression': 0.8\n    });\n  }\n}\n```\n\n**Inoutbox POS의 오프라인 동기화:**\n\n가장 복잡했던 것은 '네트워크 불안정한 매장 환경'에서의 데이터 일관성이었습니다:\n\n```dart\n// Hive DB + Network queue 패턴\nclass OfflineManager {\n  final _pendingQueue = Queue<Transaction>();\n  \n  Future<void> processTransaction(Transaction tx) async {\n    // 로컬에 즉시 저장\n    await _localDB.store(tx);\n    \n    // 네트워크 큐에 추가\n    _pendingQueue.add(tx);\n    \n    // 백그라운드에서 동기화 시도\n    _backgroundSync();\n  }\n  \n  void _backgroundSync() async {\n    while (_pendingQueue.isNotEmpty && await _hasConnection()) {\n      final tx = _pendingQueue.removeFirst();\n      try {\n        await _apiClient.sync(tx);\n        await _localDB.markSynced(tx.id);\n      } catch (e) {\n        _pendingQueue.addFirst(tx); // 재시도를 위해 다시 추가\n        break;\n      }\n    }\n  }\n}\n```\n\n**FCM과 Local Notification 하이브리드:**\n\n```dart\n// 온라인: FCM, 오프라인: Local notification\nclass NotificationService {\n  void scheduleSmartNotification(Order order) {\n    if (_isOnline) {\n      // FCM으로 실시간 알림\n      _fcm.sendToTopic('orders', order.toJson());\n    } else {\n      // 로컬 알림 스케줄링\n      _localNotifications.schedule(\n        DateTime.now().add(Duration(minutes: 30)),\n        '주문 처리 확인 필요'\n      );\n    }\n  }\n}\n```\n\n**성과:**\n- **Battery optimization**: Background processing으로 배터리 사용량 30% 감소\n- **Crash rate**: 0.1% 미만 유지 (Firebase Crashlytics 기준)\n- **Performance**: 60fps 유지, ANR 0건\n\n**Flutter vs Native 선택 기준:**\n\n현재는 '비즈니스 로직 복잡도'를 기준으로 판단합니다:\n- **Flutter**: CRUD 중심, 빠른 개발 필요\n- **Native**: 하드웨어 접근, 극한 성능 요구",
      en: "A concise Mobile answer covering real production context, key decision trade-offs, measurable outcomes, and takeaways.",
    },
  },
  {
    id: 41,
    category1: "Frontend",
    category2: "Performance",
    question: { ko: "프론트엔드 성능 최적화 경험은 어떤가요?" },
    answer: {
      ko: "**성능 최적화에서 가장 중요한 것은 '병목 지점을 정확히 식별'하는 것입니다.**\n\n**SK Drone Platform: 대용량 3D 렌더링 최적화**\n\n**문제의 본질:**\n\nChrome DevTools Performance 탭에서 발견한 병목:\n\n```javascript\n// Before: 단일 스레드에서 50MB 이미지 처리\nfunction loadDroneImage(imageUrl) {\n  const img = new Image();\n  img.onload = () => {\n    // Main thread blocking: 2.3초\n    const canvas = document.createElement('canvas');\n    const ctx = canvas.getContext('2d');\n    ctx.drawImage(img, 0, 0); // 🔥 Main thread freeze\n  };\n}\n```\n\n**Three.js LOD (Level of Detail) 수학적 구현:**\n\n```javascript\n// 카메라 거리와 화면 점유율 기반 동적 해상도\nclass AdaptiveLOD {\n  calculateOptimalResolution(camera, object) {\n    const distance = camera.position.distanceTo(object.position);\n    const screenCoverage = this.getScreenCoverage(object, camera);\n    \n    // 수학적 모델: 거리의 제곱에 반비례\n    const lodLevel = Math.max(\n      0.1, \n      Math.min(1.0, screenCoverage / (distance * distance))\n    );\n    \n    return {\n      width: Math.floor(4096 * lodLevel),\n      height: Math.floor(3072 * lodLevel)\n    };\n  }\n\n  getScreenCoverage(object, camera) {\n    // ViewFrustum과 Object bounding box 교집합 계산\n    const frustum = new THREE.Frustum();\n    frustum.setFromProjectionMatrix(camera.projectionMatrix);\n    return frustum.intersectsBox(object.boundingBox) ? 0.8 : 0.1;\n  }\n}\n```\n\n**WebWorker + OffscreenCanvas 파이프라인:**\n\n```javascript\n// Main thread\nclass ImageProcessor {\n  async processLargeImage(imageData) {\n    // Worker에서 처리, Main thread non-blocking\n    const worker = new Worker('image-processor.js');\n    \n    return new Promise((resolve) => {\n      worker.postMessage({\n        imageData,\n        operations: ['resize', 'compress', 'webp-convert']\n      });\n      \n      worker.onmessage = (e) => {\n        resolve(e.data.processedImage);\n        worker.terminate(); // 메모리 해제\n      };\n    });\n  }\n}\n\n// Worker thread (image-processor.js)\nself.onmessage = async (e) => {\n  const { imageData, operations } = e.data;\n  \n  // OffscreenCanvas로 GPU 가속 활용\n  const canvas = new OffscreenCanvas(1024, 768);\n  const ctx = canvas.getContext('2d');\n  \n  // Progressive JPEG → WebP 변환\n  const webpBlob = await canvas.convertToBlob({\n    type: 'image/webp',\n    quality: 0.8\n  });\n  \n  self.postMessage({ processedImage: webpBlob });\n};\n```\n\n**메모리 관리와 GC 최적화:**\n\n```javascript\n// Object pooling으로 GC pressure 감소\nclass TexturePool {\n  constructor() {\n    this.available = [];\n    this.inUse = new Set();\n  }\n\n  getTexture(width, height) {\n    let texture = this.available.find(t => \n      t.image.width === width && t.image.height === height\n    );\n    \n    if (!texture) {\n      texture = new THREE.Texture();\n      texture.image = { width, height };\n    } else {\n      this.available.splice(this.available.indexOf(texture), 1);\n    }\n    \n    this.inUse.add(texture);\n    return texture;\n  }\n\n  releaseTexture(texture) {\n    this.inUse.delete(texture);\n    this.available.push(texture);\n    \n    // Pool 크기 제한 (메모리 누수 방지)\n    if (this.available.length > 50) {\n      const excess = this.available.splice(0, 10);\n      excess.forEach(t => t.dispose());\n    }\n  }\n}\n```\n\n**성능 측정 결과:**\n\n```\n### Before Optimization\nFirst Contentful Paint: 4.2s\nLargest Contentful Paint: 8.1s\nTime to Interactive: 12.3s\nMain Thread Blocking: 2.3s\n\n### After Optimization\nFirst Contentful Paint: 1.1s (-74%)\nLargest Contentful Paint: 2.4s (-70%)\nTime to Interactive: 3.2s (-74%)\nMain Thread Blocking: 0.1s (-96%)\n```\n\n**Core Web Vitals 최적화 전략:**\n\n1. **LCP 개선**: Critical resource preloading\n2. **FID 개선**: 이벤트 핸들러 최적화, passive listeners\n3. **CLS 개선**: Layout shift 방지용 skeleton UI\n\n**실제 프로덕션에서 배운 교훈:**\n\n1. **Safari의 WebGL 메모리 제한**: iOS는 256MB, 별도 최적화 필요\n2. **Android Chrome의 하드웨어 가속**: GPU 메모리 부족 시 CPU 폴백\n3. **네트워크 지연**: Progressive loading이 UX에 미치는 실제 영향\n\n**현재 운영 중인 성능 모니터링:**\n\n```javascript\n// Real User Monitoring\nclass PerformanceTracker {\n  trackWebVitals() {\n    // Core Web Vitals 실시간 수집\n    getCLS(console.log);\n    getFID(console.log);\n    getLCP(console.log);\n    \n    // Custom metrics\n    this.trackImageLoadTime();\n    this.trackRenderingFPS();\n  }\n}\n```\n\n핵심은 '사용자가 실제로 느끼는 성능'을 측정하고 개선하는 것입니다.",
      en: "A concise Performance answer covering real production context, key decision trade-offs, measurable outcomes, and takeaways.",
    },
  },
];
